#include "binary_search.h"

// está função utiliza duas passagens de uma versão modificada de uma pesquisa binária para encontrar o primeiro e
// último indíce em que o elemento procurado aparece e criar o vetor de retorno a partir daí 
size_t *binary_search(void *ptr, size_t num, size_t size, void *data, int (*compare)(const void *, const void *))
{
	
}